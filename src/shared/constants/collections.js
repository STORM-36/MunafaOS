/**
 * Firestore collection name constants.
 * Always import from here — never hardcode collection strings inline.
 */

export const COLLECTIONS = {
  ORDERS: 'orders',
  INVENTORY: 'inventory',
  USERS: 'users',
  AUDIT_LOGS: 'audit_logs',
};
